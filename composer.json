{
    "name": "torifat/graphql-php",
    "description": "A GraphQL implementation for PHP",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Rifat Nabi",
            "email": "to.rifat@gmail.com"
        }
    ],
    "require": {
        "php": ">=5.6.0",
        "doctrine/lexer": "^1.0"
    },
    "autoload": {
        "psr-4": {
            "GraphQL\\": "lib/"
        }
    },
    "require-dev": {
        "phpspec/phpspec": "^2.2"
    },
    "config": {
        "bin-dir": "bin"
    },
    "scripts": {
        "test": "bin/phpspec run --config .phpspec.yml"
    }
}
